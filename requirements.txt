# Topografie-Generator – Python-Abhängigkeiten
# Installieren mit: pip install -r requirements.txt

Flask==3.1.3          # Web-Framework & Template-Engine
numpy==2.4.3          # Numerische Berechnungen
scipy==1.17.1         # gaussian_filter für fBm-Rauschen
matplotlib==3.10.8    # Konturlinien-Rendering (Agg-Backend)
pillow==12.2.0        # Bildverarbeitung für Heightmap-Upload
flask-talisman==1.1.0 # Security Headers & CSRF Protection
gunicorn==23.0.0      # Produktions-WSGI-Server für Container
rq                    # Redis Queue (optional für async workers)
redis>=5.0.0,<6       # Redis client for Python (5.x series compatible)
